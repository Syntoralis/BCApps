source "https://rubygems.org"

# csv is no longer bundled with Ruby >= 3.4; ensure it's available for older Jekyll
gem "csv"

# bigdecimal moved out of the stdlib in Ruby >= 3.4; include it for Liquid/Jekyll
gem "bigdecimal"

gem "github-pages", group: :jekyll_plugins
